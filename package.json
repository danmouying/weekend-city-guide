{
  "name": "weekend-city-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.mjs && node server.mjs",
    "start": "node build.mjs && node server.mjs",
    "test": "node --test core.test.mjs",
    "check": "node --check app.js && node --check data.js && node --check core.js",
    "build": "node build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
